# Claude Global Instructions

Use the shared governance rules in `governance/` and shared skills in `skills/`.

For non-trivial work, prepare a plan first and wait for `APPROVED TO CODE` before editing files.
