# Listing Agent Project Template

Use workflows under `workflows/ecommerce/` and skills under `skills/ecommerce/`.

Do not upload live marketplace listings unless a workflow explicitly allows it and Jim approves.
