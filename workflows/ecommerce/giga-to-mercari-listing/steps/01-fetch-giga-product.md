# Step — Fetch Giga Product

## Purpose

Fetch product data by `giga_product_id`.

## Input

Defined by previous step or workflow input.

## Output

`raw_giga_product`

## Must not

- Do not use stale cached data unless explicitly marked.
