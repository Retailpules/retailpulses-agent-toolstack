# Plan Before Code

For non-trivial tasks, agents must propose a plan before editing files.

Coding may start only after Jim comments exactly:

```text
APPROVED TO CODE
```

Before approval, agents may inspect files and draft plans. They must not edit files, commit, push, tag, deploy, or change credentials.
