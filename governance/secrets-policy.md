# Secrets Policy

Never commit real credentials, tokens, `.env`, `.mcp.json`, OAuth sessions, customer data, or marketplace exports containing private data.

Use `.env.example` and `*.example.json` files to document required variables.
